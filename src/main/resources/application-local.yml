spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookcycle?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: bookuser
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: foryu98@gmail.com
    password: izix ujno kvul afec
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
